{
  "schemaVersion": 1,
  "id": "template",
  "version": "${version}",
  "license": "BSD-3-Clause",
  "environment": "server",
  "entrypoints": {
    "main": [
      "ru.abramium.template.Template"
    ]
  },
  "mixins": [
    "template.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
